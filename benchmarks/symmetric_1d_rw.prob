
types
    x : Finite(0,10)
    y : Finite(0,1)
end

x = 0
y = Bernoulli(p)
while x > 0:
    b = Bernoulli(p)
    if b == 1:
        x = x + 1
        y = Bernoulli(0.7)
    else:
        x = x - 1
    end
end